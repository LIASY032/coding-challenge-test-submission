import { ButtonHTMLAttributes } from 'react';

// zh: 按钮类型仅允许三种合法取值；en: restrict button types to valid values only
export type ButtonType = 'button' | 'submit' | 'reset';
export type ButtonVariant = 'primary' | 'secondary';
